vagrant up
START cmd /k vagrant ssh -c 'cd /vagrant; sudo su'
TIMEOUT 5
START cmd /k vagrant ssh -c 'cd /vagrant; sudo su'
TIMEOUT 5
START cmd /k vagrant ssh -c 'cd /vagrant; sudo su'
TIMEOUT 5
START cmd /k vagrant ssh -c 'cd /vagrant; sudo su'
